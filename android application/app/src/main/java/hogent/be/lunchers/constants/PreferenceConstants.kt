package hogent.be.lunchers.constants

/**
 * De naam hoe shared preferences worden opgeslagen.
 * */
const val PREFERENCES_NAME = "lunchersPreferences"

/**
 * De naam waarmee de token in de shared preferences wordt opgeslagen.
 * */
const val PREFERENCE_TOKEN = "lunchersToken"

/**
 * De naam waarmee de gebruikersnaam in de shared preferences wordt opgeslagen.
 * */
const val PREFERENCE_USERNAME = "lunchersGebruikersnaam"

/**
 * De naam waarmee de standaard startpagina in de shared preferences wordt opgeslagen.
 * */
const val PREFERENCE_DEFAULTBOOTPAGE = "lunchersDefaultBootPage"

/**
 * De naam waarmee de standaard filtermethode in de shared preferences wordt opgeslagen.
 * */
const val PREFERENCE_DEFAULTFILTERMETHOD = "lunchersDefaultFilterMethod"